import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.likhith.deploy',
  appName: 'ngcap',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
